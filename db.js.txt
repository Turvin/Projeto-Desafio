const { Pool } = require("pg");

const pool = new Pool({
  user: "postgres",
  host: "localhost",
  database: "ordersdb",
  password: "SUA_SENHA_AQUI",
  port: 5432,
});

module.exports = pool;